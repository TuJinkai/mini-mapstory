# 阶段二：战斗系统与怪物AI — 详细设计文档

## 1. 目标概述

阶段二聚焦于 **"能打怪、怪会动、有经验值"** 三大核心目标：

| 目标 | 具体内容 |
|------|---------|
| 能打怪 | 玩家拥有普攻和技能，可对怪物造成伤害，伤害数字浮动显示 |
| 怪会动 | 怪物在地图上巡逻，检测到玩家后追击/攻击，被攻击后反击，死亡后掉落经验值 |
| 有经验值 | 击杀怪物获得经验值，经验值满后升级，属性提升，UI 实时更新 |

**边界说明（本阶段不做）：**
- 装备系统（阶段三）
- 职业系统/转职（阶段四），当前默认战士角色
- 多技能树，当前只实现：普攻 + 1个主动技能（重击）
- 怪物种类只做 2-3 种，验证系统可扩展即可

---

## 2. 文件变更清单

### 2.1 新增文件

```
src/
├── objects/
│   └── Monster.ts              # 怪物基类（属性、AI状态机、巡逻/追击/攻击）
├── systems/
│   ├── CombatSystem.ts         # 战斗系统（伤害计算、攻击检测、技能管理）
│   ├── DamageNumber.ts         # 伤害数字浮动显示
│   ├── ExpSystem.ts            # 经验值与升级逻辑
│   └── MonsterSpawner.ts       # 怪物生成与刷新管理
├── data/
│   └── MonsterData.ts          # 怪物配置表（类型、属性、AI参数）
```

### 2.2 修改文件

| 文件 | 修改内容 |
|------|---------|
| `src/utils/constants.ts` | 新增战斗相关常量（攻击范围、冷却时间、伤害公式系数等） |
| `src/objects/Player.ts` | 新增玩家战斗属性（hp/mp/atk/def）、攻击方法、受伤方法、死亡处理 |
| `src/scenes/BootScene.ts` | 新增怪物占位素材生成、攻击特效素材 |
| `src/scenes/PlayScene.ts` | 集成 CombatSystem / MonsterSpawner，添加攻击按钮，处理碰撞与战斗事件 |
| `src/scenes/UIScene.ts` | 新增技能按钮冷却显示、经验条、升级特效提示 |

---

## 3. 各模块详细设计

### 3.1 Monster 基类

**文件：** `src/objects/Monster.ts`

#### 3.1.1 怪物属性接口

```typescript
interface MonsterStats {
  hp: number;
  maxHp: number;
  atk: number;          // 攻击力
  def: number;          // 防御力
  speed: number;        // 移动速度
  expReward: number;    // 击杀经验值奖励
  detectRange: number;  // 侦测玩家范围（像素）
  attackRange: number;  // 攻击范围（像素）
  attackCooldown: number; // 攻击冷却（毫秒）
}
```

#### 3.1.2 AI 状态机

```
               玩家进入侦测范围
  ┌─────────┐ ──────────────────> ┌──────────┐
  │  PATROL  │                     │  CHASE   │
  │  巡逻    │ <────────────────── │  追击    │
  └─────────┘   玩家脱离追击范围   └──────────┘
                                    │
                     玩家进入攻击范围 │
                                    v
                                ┌──────────┐
                                │  ATTACK  │
                                │  攻击    │
                                └──────────┘
                                    │
                    受伤且hp<30%时   │
                    (逃跑型怪物)     │
                                    v
                                ┌──────────┐
                                │   FLEE   │
                                │  逃跑    │
                                └──────────┘
                                    │
                                HP <= 0
                                    v
                                ┌──────────┐
                                │   DEAD   │
                                │  死亡    │
                                └──────────┘
```

#### 3.1.3 类结构

```typescript
enum MonsterAIState {
  PATROL = 'patrol',
  CHASE = 'chase',
  ATTACK = 'attack',
  FLEE = 'flee',
  DEAD = 'dead',
}

class Monster extends Phaser.Physics.Arcade.Sprite {
  // 属性
  private stats: MonsterStats;
  private aiState: MonsterAIState = MonsterAIState.PATROL;
  private patrolOriginX: number;      // 巡逻中心点
  private patrolRange: number;        // 巡逻半径
  private lastAttackTime: number = 0; // 上次攻击时间
  private isHurt: boolean = false;    // 受击硬直

  // 核心方法
  update(player: Player): void;       // 每帧更新，根据AI状态执行行为
  takeDamage(amount: number): void;   // 受伤处理
  die(): void;                        // 死亡处理（播放动画 + 发射事件）

  // AI 行为
  private patrol(): void;             // 巡逻：在原点左右移动
  private chase(player: Player): void;// 追击：朝玩家移动
  private attack(player: Player): void;// 攻击：在攻击范围内攻击玩家
  private flee(player: Player): void; // 逃跑：远离玩家

  // 辅助
  private distanceTo(player: Player): number;
  private canAttack(): boolean;       // 冷却是否结束
  isAlive(): boolean;
}
```

#### 3.1.4 巡逻行为细节

```
patrolOriginX ────────|──────────────
                      |
           <-- patrolRange --> <-- patrolRange -->
         |___________________|___________________|
         左边界              中心             右边界

1. 怪物在 [originX - patrolRange, originX + patrolRange] 范围内左右移动
2. 移动到边界时停顿 1-2 秒后转向（模拟自然巡逻）
3. 巡逻速度为 stats.speed 的 50%
4. 朝向自动根据移动方向翻转精灵
```

#### 3.1.5 追击行为细节

```
1. 检测条件：distanceTo(player) <= stats.detectRange
2. 追击速度为 stats.speed 的 100%
3. 超出追击范围（detectRange * 1.5）后回到巡逻
4. 追击时始终朝向玩家方向移动
```

#### 3.1.6 攻击行为细节

```
1. 攻击条件：distanceTo(player) <= stats.attackRange && canAttack()
2. 攻击时短暂停顿（150ms），然后判定伤害
3. 攻击后进入冷却（stats.attackCooldown 毫秒）
4. 攻击时触发 Phaser 事件 'monster-attack'，由 CombatSystem 处理伤害
```

---

### 3.2 怪物配置表

**文件：** `src/data/MonsterData.ts`

```typescript
interface MonsterConfig {
  key: string;           // 纹理/动画键名
  stats: MonsterStats;
  patrolRange: number;   // 巡逻半径
  behavior: 'aggressive' | 'passive' | 'coward';
    // aggressive: 被打后追得更远
    // passive: 被打后反击但不远追
    // coward: HP低时逃跑
}

const MONSTER_CONFIGS: Record<string, MonsterConfig> = {
  greenSlime: {
    key: 'green_slime',
    stats: {
      hp: 50, maxHp: 50,
      atk: 8, def: 2,
      speed: 80,
      expReward: 15,
      detectRange: 150,
      attackRange: 35,
      attackCooldown: 1500,
    },
    patrolRange: 80,
    behavior: 'passive',
  },
  blueSlime: {
    key: 'blue_slime',
    stats: {
      hp: 80, maxHp: 80,
      atk: 12, def: 4,
      speed: 60,
      expReward: 25,
      detectRange: 180,
      attackRange: 35,
      attackCooldown: 2000,
    },
    patrolRange: 60,
    behavior: 'aggressive',
  },
  mushroom: {
    key: 'mushroom',
    stats: {
      hp: 35, maxHp: 35,
      atk: 6, def: 1,
      speed: 100,
      expReward: 10,
      detectRange: 120,
      attackRange: 30,
      attackCooldown: 1200,
    },
    patrolRange: 100,
    behavior: 'coward',
  },
};
```

---

### 3.3 CombatSystem（战斗系统）

**文件：** `src/systems/CombatSystem.ts`

#### 3.3.1 职责

- 接收攻击事件，计算伤害
- 管理技能冷却
- 检测攻击命中（基于距离和朝向）
- 通知 UI 更新

#### 3.3.2 伤害公式

```
基础伤害 = 攻击方.atk * (0.9 + random * 0.2)   // 90%~110% 浮动
最终伤害 = max(1, 基础伤害 - 防御方.def * 0.5)
暴击判定 = random < 暴击率(默认10%)
暴击伤害 = 最终伤害 * 1.5
```

#### 3.3.3 玩家技能定义

```typescript
interface SkillData {
  name: string;
  damageMultiplier: number;  // 伤害倍率（相对于普攻）
  mpCost: number;            // MP消耗
  cooldown: number;          // 冷却时间（毫秒）
  range: number;             // 攻击范围（像素）
  hitArea: 'front' | 'around'; // 攻击区域：前方/周围
}

const PLAYER_SKILLS: Record<string, SkillData> = {
  normalAttack: {
    name: '普通攻击',
    damageMultiplier: 1.0,
    mpCost: 0,
    cooldown: 400,      // 400ms 普攻间隔
    range: 45,
    hitArea: 'front',
  },
  powerStrike: {
    name: '重击',
    damageMultiplier: 2.5,
    mpCost: 15,
    cooldown: 3000,     // 3秒冷却
    range: 60,
    hitArea: 'front',
  },
};
```

#### 3.3.4 类结构

```typescript
class CombatSystem {
  private scene: Phaser.Scene;
  private player: Player;
  private monsters: Phaser.GameObjects.Group;
  private lastNormalAttackTime: number = 0;
  private skillCooldowns: Map<string, number>; // 技能名 -> 上次使用时间

  constructor(scene: Phaser.Scene, player: Player, monsters: Phaser.GameObjects.Group);

  // 玩家攻击
  playerAttack(skillName: string): void;
  //  - 检查冷却和MP
  //  - 确定攻击区域（玩家朝向的前方扇形/矩形区域）
  //  - 遍历范围内的怪物，对每个命中怪物调用 monster.takeDamage(damage)
  //  - 显示伤害数字（通过 DamageNumber）
  //  - 播放攻击动画/特效

  // 怪物攻击玩家
  monsterAttack(monster: Monster): void;
  //  - 计算伤害
  //  - 玩家扣血
  //  - 显示伤害数字
  //  - 更新 UI

  // 伤害计算
  private calculateDamage(atk: number, def: number, multiplier: number): { damage: number; isCrit: boolean };

  // 攻击范围检测
  private getTargetsInRange(
    attackerX: number, attackerY: number,
    facingRight: boolean, range: number, hitArea: string
  ): Monster[];

  // 工具
  isSkillReady(skillName: string): boolean;
  getSkillCooldownRemaining(skillName: string): number; // 返回剩余冷却毫秒数
}
```

#### 3.3.5 攻击范围检测示意

```
玩家朝右时的攻击区域 (hitArea = 'front', range = 45):

                  range
           |◄──────────►|
     ┌─────┬──────────────┐
     │     │  攻击判定区   │  高度 = 玩家身高
     │ 玩家 │              │
     │     │              │
     └─────┴──────────────┘
           玩家中心    玩家中心 + range

怪物中心点落入该矩形区域内则判定命中。
```

---

### 3.4 DamageNumber（伤害数字浮动显示）

**文件：** `src/systems/DamageNumber.ts`

#### 3.4.1 效果描述

```
时间线：
t=0ms      数字出现在目标头顶，初始缩放 1.5x
t=0~300ms  数字向上浮动 30px，缩放渐变到 1.0x
t=300~800ms 数字继续向上浮动 20px，透明度从 1 渐变到 0
t=800ms    数字销毁

颜色规则：
- 普通伤害：白色 (#ffffff)
- 暴击伤害：黄色 (#ffdd44)，字号更大
- 玩家受伤：红色 (#ff4444)
- 治疗/回复：绿色 (#44ff44)（预留）
```

#### 3.4.2 类结构

```typescript
class DamageNumber {
  private scene: Phaser.Scene;

  constructor(scene: Phaser.Scene);

  // 在指定位置显示伤害数字
  show(x: number, y: number, damage: number, options?: {
    color?: string;
    isCrit?: boolean;
    fontSize?: number;
  }): void;

  // 批量显示（多目标命中时）
  showMultiple(targets: Array<{ x: number; y: number; damage: number; isCrit: boolean }>): void;
}
```

#### 3.4.3 实现要点

- 使用 Phaser.Text 对象 + Tween 动画实现浮动效果
- 通过对象池复用 Text 对象（避免频繁创建销毁，性能优化）
- 设置 ScrollFactor(1) 跟随世界坐标
- Depth 设为 200，确保在所有游戏对象之上

---

### 3.5 ExpSystem（经验值与升级系统）

**文件：** `src/systems/ExpSystem.ts`

#### 3.5.1 升级公式

```typescript
// 每级所需经验值（递增公式）
function expToNextLevel(level: number): number {
  return Math.floor(50 * Math.pow(level, 1.5));
}

// 升级属性提升
const LEVEL_UP_BONUSES = {
  hp:  20,   // maxHp += 20
  mp:  10,   // maxMp += 10
  atk: 3,    // atk += 3
  def: 1,    // def += 1
};
```

#### 3.5.2 类结构

```typescript
// 升级事件数据
interface LevelUpEventData {
  newLevel: number;
  statChanges: {
    maxHp: number;
    maxMp: number;
    atk: number;
    def: number;
  };
}

class ExpSystem {
  private scene: Phaser.Scene;
  private player: Player;

  // 玩家成长属性
  private level: number = 1;
  private exp: number = 0;

  constructor(scene: Phaser.Scene, player: Player);

  // 获取当前经验值信息
  getExpInfo(): { level: number; exp: number; expToNext: number };

  // 增加经验值，返回是否升级
  addExp(amount: number): boolean;
  //  - exp += amount
  //  - while (exp >= expToNextLevel(level)) { levelUp(); }
  //  - 发射 'exp-gained' 事件
  //  - 升级时发射 'level-up' 事件

  private levelUp(): void;
  //  - level++
  //  - player.addStats(LEVEL_UP_BONUSES)
  //  - player.fullHeal()  // 升级回满血蓝
  //  - 发射 'level-up' 事件，UIScene 监听后显示升级特效
}
```

#### 3.5.3 升级经验曲线示例

| 等级 | 所需经验值 | 累计经验值 |
|------|-----------|-----------|
| 1 -> 2 | 50 | 50 |
| 2 -> 3 | 141 | 191 |
| 3 -> 4 | 259 | 450 |
| 4 -> 5 | 400 | 850 |
| 5 -> 6 | 559 | 1409 |
| 10 -> 11 | 1000 | ~4629 |

---

### 3.6 MonsterSpawner（怪物生成与刷新管理）

**文件：** `src/systems/MonsterSpawner.ts`

#### 3.6.1 生成点配置

```typescript
interface SpawnPoint {
  x: number;
  y: number;
  monsterType: string;   // 对应 MonsterData 中的 key
  respawnTime: number;   // 死亡后刷新时间（毫秒）
}

// 示例：PlayScene 中的生成点
const SPAWN_POINTS: SpawnPoint[] = [
  { x: 300, y: 400, monsterType: 'greenSlime', respawnTime: 5000 },
  { x: 500, y: 400, monsterType: 'greenSlime', respawnTime: 5000 },
  { x: 650, y: 400, monsterType: 'blueSlime',  respawnTime: 8000 },
  { x: 200, y: 260, monsterType: 'mushroom',   respawnTime: 4000 },
  { x: 450, y: 190, monsterType: 'mushroom',   respawnTime: 4000 },
];
```

#### 3.6.2 类结构

```typescript
class MonsterSpawner {
  private scene: Phaser.Scene;
  private spawnPoints: SpawnPoint[];
  private activeMonsters: Monster[];       // 当前活跃怪物列表
  private deathTimers: Map<string, number>; // 生成点 -> 死亡时间戳
  private monsterGroup: Phaser.GameObjects.Group;
  private player: Player;

  constructor(scene: Phaser.Scene, player: Player, spawnPoints: SpawnPoint[]);

  // 初始化：在所有生成点创建怪物
  init(): void;

  // 每帧更新：检查是否有需要刷新的怪物
  update(time: number): void;
  //  - 遍历 deathTimers，如果 time - deathTime >= respawnTime，重新生成怪物

  // 在指定生成点创建怪物
  private spawnMonster(spawnPoint: SpawnPoint): Monster;
  //  - 从 MonsterData 读取配置
  //  - new Monster(scene, x, y, config)
  //  - 添加到 physics group + activeMonsters
  //  - 设置与平台的碰撞
  //  - 监听怪物的 'death' 事件，记录到 deathTimers

  // 获取所有活跃怪物（供 CombatSystem 使用）
  getActiveMonsters(): Monster[];

  // 清理所有怪物（场景销毁时调用）
  destroy(): void;
}
```

#### 3.6.3 刷新流程

```
怪物死亡
  │
  v
从 activeMonsters 移除
  │
  v
记录 deathTimers[spawnPointId] = currentTime
  │
  v
update() 每帧检查
  │
  v
currentTime - deathTime >= respawnTime ?
  │
  ├─ 否 → 等待
  │
  └─ 是 → spawnMonster(spawnPoint)
           播放出生特效（淡入 + 闪烁 0.5秒无敌时间）
```

---

### 3.7 Player 扩展（修改现有文件）

**文件：** `src/objects/Player.ts`（修改）

#### 3.7.1 新增战斗属性

```typescript
interface PlayerStats {
  hp: number;
  maxHp: number;
  mp: number;
  maxMp: number;
  atk: number;
  def: number;
  critRate: number; // 暴击率 0~1
}

// 初始属性（战士）
const INITIAL_PLAYER_STATS: PlayerStats = {
  hp: 100, maxHp: 100,
  mp: 80,  maxMp: 80,
  atk: 15, def: 5,
  critRate: 0.1,
};
```

#### 3.7.2 新增方法

```typescript
// 新增到 Player 类
private stats: PlayerStats;
private isAttacking: boolean = false;  // 攻击动画进行中
private isDead: boolean = false;

// 战斗相关方法
takeDamage(amount: number): void;
  // stats.hp -= amount
  // 受击闪烁效果
  // 如果 hp <= 0，触发死亡

addStats(bonus: Partial<PlayerStats>): void;
  // 升级时调用，提升属性

heal(hpAmount: number, mpAmount: number): void;
  // 回复 HP/MP

fullHeal(): void;
  // HP/MP 完全回复

getStats(): PlayerStats;
getAttackBox(): Phaser.Geom.Rectangle;
  // 返回当前攻击判定区域（基于朝向和攻击范围）

isAlive(): boolean;

// 新增动画状态
private updateAnimation(onGround: boolean);
  // 增加 'attack' 和 'hurt' 状态判断
```

---

### 3.8 常量扩展

**文件：** `src/utils/constants.ts`（修改）

```typescript
// ===== 新增战斗相关常量 =====
export const ATTACK_RANGE = 45;            // 普攻范围（像素）
export const SKILL_RANGE = 60;             // 技能攻击范围
export const NORMAL_ATTACK_COOLDOWN = 400;  // 普攻冷却（ms）
export const POWER_STRIKE_COOLDOWN = 3000;  // 重击冷却（ms）
export const POWER_STRIKE_MP_COST = 15;     // 重击MP消耗
export const POWER_STRIKE_MULTIPLIER = 2.5; // 重击伤害倍率
export const CRIT_RATE = 0.1;              // 默认暴击率 10%
export const CRIT_MULTIPLIER = 1.5;        // 暴击伤害倍率
export const DAMAGE_FLOAT_DURATION = 800;   // 伤害数字持续时间（ms）
export const HURT_INVINCIBLE_TIME = 500;    // 受伤无敌时间（ms）
export const MONSTER_SPAWN_INVINCIBLE = 500; // 怪物出生无敌时间（ms）
export const RESPAWN_CHECK_INTERVAL = 1000;  // 刷新检查间隔（ms）

// MP 自然回复
export const MP_REGEN_RATE = 2;   // 每 3 秒回复 2 点 MP
export const MP_REGEN_INTERVAL = 3000;
```

---

### 3.9 BootScene 素材扩展

**文件：** `src/scenes/BootScene.ts`（修改）

新增占位素材生成方法：

```
generateMonsterSprites(): void;
  // green_slime: 绿色 24x24 精灵，2帧（idle 呼吸动画）
  // blue_slime:  蓝色 24x24 精灵，2帧
  // mushroom:    橙色 20x24 精灵，2帧
  // 每种怪物配 idle + hurt + die 三组动画

generateEffectSprites(): void;
  // slash_effect: 白色弧线，3帧攻击特效
  // level_up_effect: 金色光环，4帧升级特效

// 怪物动画注册
createMonsterAnimations(): void;
  // 为每种怪物创建 anims:
  //   {key}_idle:  2帧循环，frameRate=4
  //   {key}_hurt:  1帧，不循环
  //   {key}_die:   2帧，不循环，播放完毕停最后一帧

// 玩家新动画
addPlayerAnimations(): void;
  // player_attack: 3帧，不循环
  // player_hurt:   1帧，不循环
```

---

### 3.10 PlayScene 战斗集成

**文件：** `src/scenes/PlayScene.ts`（修改）

```typescript
class PlayScene extends Scene {
  private combatSystem!: CombatSystem;
  private monsterSpawner!: MonsterSpawner;
  private expSystem!: ExpSystem;
  private damageNumber!: DamageNumber;

  create() {
    // ... 现有代码保持 ...

    // 新增：初始化战斗系统
    this.monsterSpawner = new MonsterSpawner(this, this.player, SPAWN_POINTS);
    this.monsterSpawner.init();

    const monsterGroup = this.monsterSpawner.getMonsterGroup();
    this.combatSystem = new CombatSystem(this, this.player, monsterGroup);
    this.expSystem = new ExpSystem(this, this.player);
    this.damageNumber = new DamageNumber(this);

    // 碰撞：怪物与平台
    this.physics.add.collider(monsterGroup, this.platforms);

    // 事件监听
    this.setupCombatEvents();

    // 新增：攻击按钮
    this.createAttackControls();

    // 场景间通信：将 expSystem 引用传给 UIScene
    this.events.emit('combat-ready', { expSystem, combatSystem });
  }

  update(time: number) {
    this.player.update(this.cursors);
    this.monsterSpawner.update(time);
    this.updateMonsters();
  }

  private setupCombatEvents(): void {
    // 监听怪物死亡 → 给经验
    this.events.on('monster-death', (monster: Monster) => {
      const exp = monster.getExpReward();
      const leveledUp = this.expSystem.addExp(exp);
      // 显示获得经验值数字
      this.damageNumber.show(monster.x, monster.y - 20, `+${exp}`, {
        color: '#44ff44', fontSize: 14,
      });
    });

    // 监听升级 → 通知 UIScene
    this.events.on('level-up', (data: LevelUpEventData) => {
      // UIScene 会监听这个事件
    });

    // 监听玩家受伤 → 通知 UIScene 更新血条
    this.events.on('player-hurt', () => {
      this.updateUI();
    });
  }

  private updateMonsters(): void {
    for (const monster of this.monsterSpawner.getActiveMonsters()) {
      monster.update(this.player);
    }
  }

  private createAttackControls(): void {
    const buttonY = GAME_HEIGHT - 50;

    // 普攻按钮（攻击按钮旁边）
    const attackBtn = this.add.circle(GAME_WIDTH - 130, buttonY, 26, 0xff4444, 0.3)
      .setScrollFactor(0).setDepth(100).setInteractive();
    this.add.text(GAME_WIDTH - 130, buttonY, 'ATK', {
      fontSize: '12px', color: '#ffffff', fontStyle: 'bold',
    }).setOrigin(0.5).setScrollFactor(0).setDepth(101);

    // 技能按钮（重击）
    const skillBtn = this.add.circle(GAME_WIDTH - 60, buttonY - 40, 24, 0xffaa00, 0.3)
      .setScrollFactor(0).setDepth(100).setInteractive();
    this.add.text(GAME_WIDTH - 60, buttonY - 40, 'SKL', {
      fontSize: '11px', color: '#ffffff', fontStyle: 'bold',
    }).setOrigin(0.5).setScrollFactor(0).setDepth(101);

    // 触摸事件
    this.input.on('gameobjectdown', (_pointer, gameObject) => {
      if (gameObject === attackBtn) {
        this.combatSystem.playerAttack('normalAttack');
      }
      if (gameObject === skillBtn) {
        this.combatSystem.playerAttack('powerStrike');
      }
    });

    // 键盘支持（可选：Z=普攻, X=技能）
    // 在 update 中检测键盘输入
  }

  private updateUI(): void {
    // 通过 scene 事件或直接引用通知 UIScene
    const stats = this.player.getStats();
    const expInfo = this.expSystem.getExpInfo();
    this.scene.get('UIScene').events.emit('update-stats', {
      hp: stats.hp,
      maxHp: stats.maxHp,
      mp: stats.mp,
      maxMp: stats.maxMp,
      level: expInfo.level,
      exp: expInfo.exp,
      expToNext: expInfo.expToNext,
    });
  }
}
```

---

### 3.11 UIScene 升级

**文件：** `src/scenes/UIScene.ts`（修改）

新增内容：
- 经验值进度条（HP/MP 下方）
- 技能冷却遮罩显示（与 PlayScene 中的技能按钮联动）
- 升级闪光特效提示
- 监听 PlayScene 的 'update-stats' 事件实时更新

---

## 4. 模块交互流程

### 4.1 玩家攻击怪物 — 时序流程

```
[玩家点击 ATK 按钮]
       │
       v
PlayScene.createAttackControls() ──> 触摸事件回调
       │
       v
CombatSystem.playerAttack('normalAttack')
       │
       ├── 检查冷却 ─── 未就绪 ──> return（忽略）
       │
       ├── 检查MP ─── MP不足 ──> return（可选：提示MP不足）
       │
       ├── 扣除MP（如有消耗）
       │
       ├── 记录冷却时间
       │
       ├── 播放玩家攻击动画
       │
       ├── 计算攻击区域
       │
       ├── 遍历范围内怪物
       │       │
       │       v
       │   calculateDamage(player.atk, monster.def, multiplier)
       │       │
       │       ├── 随机浮动 (90%~110%)
       │       ├── 减防 (def * 0.5)
       │       ├── 暴击判定 (10%, 1.5x)
       │       │
       │       v
       │   monster.takeDamage(finalDamage)
       │       │
       │       ├── 扣血 + 受击动画
       │       │
       │       ├── DamageNumber.show(x, y, damage)
       │       │       └── 浮动数字动画
       │       │
       │       └── hp <= 0 ?
       │            ├── 是 → monster.die()
       │            │         └── 发射 'monster-death' 事件
       │            │                   │
       │            │                   v
       │            │         PlayScene 收到事件
       │            │                   │
       │            │                   v
       │            │         ExpSystem.addExp(expReward)
       │            │                   │
       │            │                   ├── 更新经验值
       │            │                   ├── 通知 UIScene 更新经验条
       │            │                   └── 升级? → player.addStats()
       │            │                            → fullHeal()
       │            │                            → 发射 'level-up' 事件
       │            │                            → UIScene 显示升级特效
       │            │
       │            └── 否 → 怪物进入 hurt 状态 → AI 切换
       │
       └── 更新 UIScene (MP变化)
```

### 4.2 怪物攻击玩家 — 时序流程

```
[Monster AI 每帧 update]
       │
       ├── 距离 > detectRange → PATROL（巡逻）
       ├── 距离 > attackRange → CHASE（追击）
       └── 距离 <= attackRange && 冷却结束
               │
               v
       Monster.attack()
       AI状态 = ATTACK
       记录攻击时间
               │
               v
       发射 'monster-attack' 事件
               │
               v
       CombatSystem.monsterAttack(monster)
               │
               ├── calculateDamage(monster.atk, player.def, 1.0)
               │
               ├── player.takeDamage(damage)
               │       ├── 扣血 + 受击闪烁
               │       ├── DamageNumber.show(player.x, player.y, damage, red)
               │       └── hp <= 0 ? → 玩家死亡处理
               │
               └── 通知 UIScene 更新血条
```

### 4.3 怪物刷新流程

```
[MonsterSpawner.update(time) 每帧调用]
       │
       v
遍历 deathTimers
       │
       v
对每个死亡记录：
  time - deathTime >= respawnTime ?
       │
       ├── 否 → 跳过
       │
       └── 是 → spawnMonster(spawnPoint)
               │
               ├── new Monster(scene, x, y, config)
               ├── 设置物理碰撞
               ├── 出生无敌 500ms
               ├── 淡入特效
               └── 加入 activeMonsters
```

---

## 5. 建议实现顺序（子阶段划分）

### 子阶段 2.1：怪物基础（预计 1-2 小时）

**目标：** 地图上出现能巡逻的怪物

| 步骤 | 内容 | 文件 |
|------|------|------|
| 1 | 新增怪物相关常量到 constants.ts | `src/utils/constants.ts` |
| 2 | 创建怪物配置表 | `src/data/MonsterData.ts` |
| 3 | BootScene 生成怪物占位素材 + 动画 | `src/scenes/BootScene.ts` |
| 4 | 实现 Monster 类（仅 PATROL 状态） | `src/objects/Monster.ts` |
| 5 | PlayScene 中创建一个测试怪物，验证巡逻 | `src/scenes/PlayScene.ts` |

**验证标准：** 启动游戏后可以看到怪物在地图上左右巡逻，会翻转方向。

---

### 子阶段 2.2：战斗系统 — 玩家攻击（预计 1-2 小时）

**目标：** 玩家可以攻击怪物，怪物受伤死亡

| 步骤 | 内容 | 文件 |
|------|------|------|
| 1 | Player 类增加战斗属性和方法 | `src/objects/Player.ts` |
| 2 | BootScene 增加攻击动画和特效素材 | `src/scenes/BootScene.ts` |
| 3 | 实现 DamageNumber 伤害数字显示 | `src/systems/DamageNumber.ts` |
| 4 | 实现 CombatSystem（仅玩家攻击部分） | `src/systems/CombatSystem.ts` |
| 5 | PlayScene 增加攻击按钮，集成战斗系统 | `src/scenes/PlayScene.ts` |

**验证标准：** 点击 ATK 按钮可以在怪物头上打出伤害数字，怪物血量归零后消失。

---

### 子阶段 2.3：怪物AI完整化（预计 1-2 小时）

**目标：** 怪物会追击和攻击玩家

| 步骤 | 内容 | 文件 |
|------|------|------|
| 1 | Monster 增加 CHASE / ATTACK / FLEE 状态 | `src/objects/Monster.ts` |
| 2 | CombatSystem 增加 monsterAttack 方法 | `src/systems/CombatSystem.ts` |
| 3 | Player 增加 takeDamage + 受击效果 | `src/objects/Player.ts` |
| 4 | Player 增加受伤无敌帧机制 | `src/objects/Player.ts` |

**验证标准：** 怪物检测到玩家后追过来攻击，玩家受伤后血条减少，有受击闪烁效果。

---

### 子阶段 2.4：经验值与升级 + 刷新管理（预计 1-2 小时）

**目标：** 完整的打怪→升级循环

| 步骤 | 内容 | 文件 |
|------|------|------|
| 1 | 实现 ExpSystem 经验值与升级逻辑 | `src/systems/ExpSystem.ts` |
| 2 | 实现 MonsterSpawner 怪物刷新管理 | `src/systems/MonsterSpawner.ts` |
| 3 | 连接死亡事件 → 经验值 → 升级 → UI | `src/scenes/PlayScene.ts` |
| 4 | UIScene 增加经验条 + 升级提示 | `src/scenes/UIScene.ts` |
| 5 | 增加多个生成点，多种怪物 | `src/scenes/PlayScene.ts` |

**验证标准：** 杀怪获得经验值，经验值满后升级，属性提升，血蓝回满，怪物死后一段时间重新刷新。

---

### 子阶段 2.5：技能系统 + 打磨（预计 1-2 小时）

**目标：** 重击技能、冷却显示、整体体验优化

| 步骤 | 内容 | 文件 |
|------|------|------|
| 1 | 实现重击技能（范围、伤害倍率、MP消耗、冷却） | `src/systems/CombatSystem.ts` |
| 2 | UIScene 技能冷却遮罩显示 | `src/scenes/UIScene.ts` |
| 3 | MP 自然回复机制 | `src/objects/Player.ts` / `src/scenes/PlayScene.ts` |
| 4 | 攻击特效动画（挥砍弧线） | `src/scenes/BootScene.ts` + `src/systems/CombatSystem.ts` |
| 5 | 键盘快捷键支持（Z=普攻, X=技能） | `src/scenes/PlayScene.ts` |
| 6 | 整体数值平衡调整 | `src/data/MonsterData.ts` + `src/utils/constants.ts` |

**验证标准：** 拥有普攻+重击两个技能，有明显的冷却感，MP 消耗和回复正常，整体打怪体验流畅。

---

## 6. 数据流向总览

```
┌─────────────────────────────────────────────────────────────────┐
│                          PlayScene                               │
│                      (总调度/事件总线)                             │
│                                                                   │
│  ┌──────────┐    events     ┌───────────────┐    events    ┌───┐ │
│  │ Player   │◄─────────────│ CombatSystem   │────────────►│UI │ │
│  │          │  takeDamage   │               │  update-stats│SC │ │
│  │ stats:   │               │ playerAttack  │              │en │ │
│  │ hp/mp/   │               │ monsterAttack │              │e  │ │
│  │ atk/def  │               │ calculateDmg  │              └───┘ │
│  └──────────┘               └───────────────┘                    │
│       ▲                            │                              │
│       │                            │ hit detection               │
│       │                            v                              │
│       │                    ┌───────────────┐                      │
│       │                    │ MonsterSpawner│                      │
│       │                    │               │                      │
│       │                    │ spawnMonster  │───────┐              │
│       │                    │ update(respawn)│      │              │
│       │                    └───────────────┘      │              │
│       │                            │ creates      │              │
│       │                            v              v              │
│       │                    ┌───────────────┐                     │
│       │                    │   Monster[]   │                     │
│       │                    │               │                     │
│       └────────────────────│ update(AI)    │                     │
│          attack events     │ takeDamage    │                     │
│                          └───────────────┘                      │
│                                │                                  │
│                          death event                             │
│                                │                                  │
│                                v                                  │
│                        ┌───────────────┐                          │
│                        │  ExpSystem    │                          │
│                        │              │                          │
│                        │ addExp()     │──► levelUp ──► Player    │
│                        │ getExpInfo() │──► UIScene              │
│                        └───────────────┘                          │
│                                                                   │
│            ┌───────────────┐                                      │
│            │ DamageNumber  │◄── CombatSystem / Monster            │
│            │ show(x,y,dmg) │    (多处调用)                        │
│            └───────────────┘                                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 7. 注意事项

### 7.1 性能考量

- **怪物数量上限：** 单屏不超过 8 个活跃怪物，避免 AI 计算和碰撞检测压力
- **伤害数字对象池：** 复用 Text 对象，避免频繁 GC
- **AI 更新频率：** 怪物 AI 每帧都执行，但距离检测使用简单的 Math.abs(dx) 而非完整碰撞检测
- **攻击范围检测：** 使用矩形区域判定，不使用物理重叠检测（更可控）

### 7.2 移动端适配

- 攻击/技能按钮大小 >= 48px 直径（满足触摸目标最小尺寸）
- 按钮位置避免与方向键/跳跃键冲突
- 技能冷却通过半透明遮罩 + 倒计时文字直观反馈

### 7.3 扩展性预留

- Monster 类通过 MonsterConfig 配置，新增怪物种类只需添加配置项
- CombatSystem 的技能通过 SkillData 定义，后续添加新技能只需扩展配置
- ExpSystem 的升级奖励通过 LEVEL_UP_BONUSES 常量控制，后续可按职业差异化
- 所有模块间通过 Phaser EventEmitter 解耦，便于后续添加装备系统（修改伤害计算）和职业系统（修改技能列表）
